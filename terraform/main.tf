# (AWS / Learner Lab edition) The local Docker-provider resources were removed
# when this project was switched back to AWS. All infrastructure now lives in
# vpc.tf, security_groups.tf, alb.tf, ec2.tf, bastion.tf and outputs.tf.
# This file is intentionally left empty.
