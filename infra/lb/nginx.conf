# Placeholder — Terraform overwrites this with the rendered load-balancer
# config (see terraform/main.tf, resource local_file.lb_conf) on apply.
events {}
http {
  server { listen 80; location / { return 503 "not provisioned yet\n"; } }
}
